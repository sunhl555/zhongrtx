# 酒店旅拍助手 · 部署说明

---

## 一、本地使用（立即体验）

1. 双击打开 `旅拍助手.html`
2. 输入密码 `888888` 进入管理端
3. 新建模板 → 填写文案 → 上传图片 → 保存
4. 点击「导出客人页」→ 得到一个 `guest.html` 文件
5. 用浏览器打开 `guest.html` 即可看到客人视角

---

## 二、部署到 GitHub Pages（让客人扫码使用）

### 第一步：注册 GitHub 账号

1. 打开 https://github.com
2. 点击右上角 **Sign up** 注册账号
3. 用户名建议：`zhongrui-hotel` 或你的酒店拼音

### 第二步：创建仓库

1. 登录后，点击右上角 **+** → **New repository**
2. Repository name 填：`lp` （或任意名字）
3. 勾选 **Public**（公开）
4. 点击 **Create repository**

### 第三步：上传文件

1. 进入刚创建的仓库页面
2. 点击 **uploading an existing file** 链接
3. 将 `guest.html` 拖拽到浏览器窗口
4. 下拉到页面底部，点击 **Commit changes**

### 第四步：启用 GitHub Pages

1. 进入仓库 → 顶部点 **Settings**
2. 左侧菜单点 **Pages**
3. Branch 选择 **main**（或 master），点 **Save**
4. 等待约 1 分钟后，页面显示：
   > Your site is published at `https://你的用户名.github.io/lp/guest.html`

### 第五步：回到管理端设置

1. 打开 `旅拍助手.html`
2. 进入管理端 → 选择模板
3. 编辑页面中找到 **QR 设置**，填入你的 GitHub Pages 地址（带 #guest?id=模板ID）
4. 打印二维码贴在前台，客人退房时扫码即可

---

## 三、客人使用流程

```
退房时扫二维码
      ↓
 看到准备好的图文
      ↓
 点「一键复制文案」
      ↓
 长按图片保存到手机
      ↓
 点「打开携程发旅拍」
      ↓
 粘贴文案 + 上传图片 → 发布
```

客人全程只需约 30 秒。

---

## 四、日常维护

| 频率 | 操作 |
|------|------|
| **每周** | 登录管理端，更换模板内容（不同季节/活动用不同文案） |
| **内容更新后** | 点「导出客人页」，将新的 `guest.html` 上传到 GitHub |
| **每月** | 检查二维码是否正常，链接是否有效 |

---

## 五、常见问题

**Q: 客人扫码后打不开页面？**
A: 检查 GitHub Pages 是否启用成功，链接是否正确。

**Q: 图片加载很慢？**
A: 上传图片时工具会自动压缩到合适大小。如果还是慢，建议图片不要超过 500KB/张。

**Q: 可以同时管理多套模板吗？**
A: 可以。比如春节用一套，暑期用一套，切换模板后重新导出即可。

**Q: 需要付费吗？**
A: 完全免费。GitHub Pages 免费托管，无流量限制。

**Q: 忘记管理密码怎么办？**
A: 在浏览器中清除 `localhost` 或 `file://` 的 localStorage 即可恢复默认密码 `888888`。
